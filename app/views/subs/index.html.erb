<h3>登録サービス一覧</h3><br/>

<%# サービスが登録されていれば %>
<% if @subs.count != 0 %>
  <p>
    <%= "月々の料金は" + @total_fee.to_s + "円です。" %>
  </p>
  <br/>

  <table class="table table-striped table-light table-hover">
    <thead class="table-dark">
      <tr align="center">
        <th>サービス名</th>
        <th>料金</th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
      <% @subs.each do |sub| %>
        <tr>
          <td align="center"><%= sub.sub_name %></td>
          <td align="center"><%= sub.fee.to_s + "円" + sub.period %></td>
          <td align="center"><%= link_to '編集', edit_sub_path(sub), class:"btn btn-link btn-sm" %></td>
          <td align="center"><%= link_to '削除', sub, method: :delete,
           data: { confirm: '削除してよろしいですか？' }, class:"btn btn-link btn-sm" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br/>

  <div class="row justify-content-evenly">
    <div class="pagiWrapper col-auto">
      <%= paginate @subs %>
    </div>
    
    <div class="col-auto">
      <%= link_to "サービスを追加登録",  new_sub_path, 
      class: "btn btn-success text-nowrap" %>
    </div>
  </div>
  

<%# 一つも登録がなければ %>
<% else %>
  <p>サービスが登録されていません。</p><br/>
  <%= link_to  "サービスを追加登録", new_sub_path, class:"btn btn-success" %>
<% end %>